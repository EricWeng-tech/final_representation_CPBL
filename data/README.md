# Data

`raw/` contains the four CSV inputs required to validate and rebuild the model table.

`processed/model_ready_games.csv` is the table read by the model notebooks.

`cache/` contains re-downloadable API JSON and is intentionally excluded from Git.
