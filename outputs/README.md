# Outputs

`metrics/`, `figures/`, and summary files under `experiments/` are tracked for reports and review.

Row-level prediction CSV files are generated by notebooks and intentionally excluded from Git.
